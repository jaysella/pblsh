import styled from "@emotion/styled";

export default function Layout() {
  return (
    <FooterWrapper>
      {/* <p>&copy; {new Date().getFullYear()} pblsh. All Rights Reserved.</p> */}
    </FooterWrapper>
  );
}

export const FooterWrapper = styled.footer`
  padding: 2rem 5rem;
`;
